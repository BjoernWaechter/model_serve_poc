# EKS cluster for Ray
Installs an EKS cluster with installed Ray CRDs (custom resource defintion)
```
cd model_serve_poc/iac/eks-ray
terraform init
terraform apply
```

Delete cluster
```
terraform destroy
```
